{
  "name": "pingdrop",
  "version": "1.0.0",
  "description": "Live location sharing — Vercel + Upstash Redis",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.28.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
